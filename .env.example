# 이 파일을 .env 로 복사한 뒤 값을 채우세요 (cp .env.example .env)
# 또는 systemd / 쉘 환경변수로 직접 설정해도 됩니다.

# 웹훅 + 대시보드 접속 비밀값 (아무나 못 들어오게 막는 키).
# 길고 무작위로. 예: openssl rand -hex 16 로 생성.
SMS_WEBHOOK_SECRET=여기에_무작위_비밀값

# 서버 포트 (기본 8080)
PORT=8080
